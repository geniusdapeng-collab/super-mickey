/**
 * 审核门槛唯一真源（字段数）
 * 镜头总览核验、审核须知、预检规范卡等所有"字段数审核标准"必须从此处读取，
 * 禁止就地硬编码。注意：门槛是最低下限，实际字段数以
 * prompt-fusion-agent.js 解析结果为准（当前内容镜头 26、片头镜头 31）。
 */
module.exports = {
  CONTENT_MIN: 25, // 内容镜头字段数审核门槛（≥25 即 ✅）
  OPENING_MIN: 30, // 片头镜头字段数审核门槛（≥30 即 ✅）
};
