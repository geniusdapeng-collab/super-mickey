

---

## 2026-06-20 01:00 v2.0.3-audit-fix 发布

队长要求把审计报告中"需要修复的"全部修复并发布版本。

**已完成全部P0+P1修复**：

**P0 Critical（v2.0.2-security-patch）**：
• 硬编码API Key全面清理（30+文件）
• _fetchWithTimeout完整超时控制
• loadModule关键模块缺失抛明确错误
• 删除14个旧导出文件

**P1 High/Medium（v2.0.3-audit-fix）**：
1. LLM网关JSON分块合并 — JSON/structured禁用自动分块，仅text允许
2. 暴力JSON截断O(n²) → 单次栈扫描O(n)
3. NaN安全解析 — safeParseInt/safeParseFloat，失败回退默认值
4. HTML拼接XSS — escapeHtml转义，防护角色名/标题/弹幕

**P2 Low**：队长说"不适合的就不需要了"，console.log清理等暂未处理

**版本信息**：
• v2.0.2-security-patch: commit d5729eb
• v2.0.3-audit-fix: commit c7ad234
• Git Tag已推送

等待队长下一步指示。
