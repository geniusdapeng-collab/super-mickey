
## 01:15 — 第二集预生产完成

队长要求：
1. 修复LLM响应问题 ✅
2. 提交生产发布 ✅（v2.0.4本地tag）
3. 重跑预生产 ✅

**结果**：
- 6镜头，62秒，16:9横屏
- 内容：横纹肌溶解原因分析
- 风格：工业风健身工作室
- 状态：待队长审阅

**输出文件**：
- output/episode2-preproduction/blueprint.json
- output/episode2-preproduction/preproduction-report-v2.0.4.md

---

## 12:35 — v2.0.5 修复中文字段名 + 时间轴 + 定妆照 + dialogueStr 报错

队长要求解决预生产输出的5大问题：中文字段名、时间轴、定妆照、人物卡片、[object Object]。

**根因分析与修复**：

1. **时间轴太简单（2段）** ✅
   - 根因：`VisualLanguageAgent` system prompt 允许 `动态切分2-4段`
   - 修复：改为 `动态切分4-6段，根据情绪起伏设计，每段包含时间范围、运镜动作、画面目的，必须详细具体`
   - 验证：SC00-05 时间轴分别为 4、5、5、5、4、3 段

2. **定妆照路径为描述文本（"陈卓: main character"）** ✅
   - 根因1：`ScriptGenerator._overrideCharacters` 中使用 `c.id || c.name`，但用户传入的是 `character_id` 而非 `id`
   - 根因2：`ProductionEngine._buildCharacterRef` 搜索时未正确处理中文名映射
   - 修复：
     - `ScriptGenerator`: `c.id || c.name` → `c.character_id || c.id || c.name`
     - `ProductionEngine`: 使用 `char.character_id` 作为目录名
     - `Adapter`: 增强 `_resolvePortraitPaths`，支持 portraits/ 子目录
   - 验证：定妆照显示 `image://characters/chen-zhuo/front.png`

3. **dialogueStr.split is not a function** ✅
   - 根因：`FieldGuard.printShotSummary` 中 `shot.dialogue` 可能为对象而非字符串
   - 修复：强制类型转换

4. **中文字段名** ✅（已在 v2.0.6 完成）
   - 全部 11+ 字段使用【】格式，0个英文字段名残留

**版本号**: `v2.0.6-fix1` | **Git提交**: `b3c7ed8`（20 files changed, 3511 insertions, 121 deletions）

**文件变更**：
- `engines/production-engine/agents/visual-language-agent.js`
- `engines/script-engine/core/script-generator.js`
- `engines/script-engine/core/adapter.js`
- `engines/production-engine/production-engine.js`
- `engines/field-guard.js`

**队长核心要求**：
- 提示词中必须使用中文【】方括号字段名，不能用英文冒号格式（如 dialogue: / mood: / timeline:）
- 定妆照必须走标准流程：检查角色定妆照 → 没有则走生成流程 → 与用户确认 → 调用写入真实路径
- 时间轴对画面影响极其重要，必须保证复杂运镜
- 真实执行 > 看起来成功，禁止假跑

## 13:20 — v2.0.6-fix1 生产验证全部通过

队长确认方向：继续改超现实系统，不回退。

验证结果：
- 时间轴：4-5段 ✅
- 定妆照：`image://characters/chen-zhuo/front.png` ✅
- dialogueStr：0错误 ✅
- 中文字段：11个全部到位 ✅
- Git已推送云端 ✅


## 14:03 — 需求清单确认流程修复（系统级错误复盘）

**问题**：队长指出预生产任务跳过了"需求要点确认"环节。这是不可跳过的强制流程。

**根因**：`hyperreality-system/index.js` 中提供了 `skipRequirementConfirmation: true` 选项，允许自动化脚本绕过确认环节。

**修复**：
1. 移除 `skipRequirementConfirmation` 选项，需求清单确认变为强制阻塞环节
2. 系统不再接受任何跳过需求确认的调用方式
3. 写入 SOUL.md、AGENTS.md 作为永久规则

**规则强化（已写入系统）**：
- SOUL.md: 【新增】v6.6.4 视频需求清单确认流程（焊死，不可跳过，系统强制阻塞）
- AGENTS.md: 视频预生产 - 需求清单确认（强制规则）
- index.js: 移除 `skipRequirementConfirmation` 分支

**队长确认**：此次任务需求要点已确认，继续执行。

---

## 14:03 — v2.0.6-fix2：需求清单确认流程强制化（系统级修复）

队长要求：
1. 修复需求确认环节不可跳过的问题
2. 提交生产版本发布
3. 写入记忆系统、SOUL.md、AGENTS.md

**修复内容**：
- `index.js`：移除 `skipRequirementConfirmation` 选项支持，改为强制阻塞
- `SOUL.md`：更新 v6.6.4 为"焊死，不可跳过，系统强制阻塞"
- `AGENTS.md`：新增"视频预生产 - 需求清单确认（强制规则）"章节

**版本号**：`v2.0.6-fix2` | **Git提交**：`f9a2b3c`（20 files changed）

**验证**：第二集重新跑完整预生产（含需求确认环节），4镜头，65秒，通过质量门。



## 2026-06-20 17:12 - 超现实系统核心能力修复记录

### 背景
大鹏队长要求检查超现实系统中两个子系统的状态：
1. 140+ 好莱坞导演技能库是否在主链路中发挥作用
2. 创意指数是否真正影响链路模块

### 检查结果（问题严重）

#### 问题1：好莱坞导演技能 - 完全未接入 ❌
- **文件存在**: `skills/hollywood-cinematography/cinematography-skill-router.js` 包含149个导演技能
- **根因**: 超现实系统主入口 `index.js` 从未导入或调用技能路由器
- **影响**: `production-engine.js` 中预留的 `【导演】` 字段永远为空
- **验证**: 149个技能文件就绪，但主链路零调用

#### 问题2：创意指数 - 半工作状态 ⚠️
- **解析正常**: `CreativeIntensityEngine` 能正确解析创意指数（如 0.49）
- **配置生成正常**: 能生成各Layer配置（叙事结构、镜头语言、灯光设计）
- **关键断裂**: 生成的配置存储在 `metadata._creativeIntensity` 中
- **致命问题**: `ScriptGenerator._buildGenerationPrompt()` 从未读取该字段
- **结果**: LLM生成剧本时完全不知道创意指数的存在

### 修复方案与实施

#### 修复1：创意指数注入 LLM Prompt
**修改文件**: `hyperreality-system/engines/script-engine/core/script-generator.js`
**修改位置**: `_buildGenerationPrompt()` 方法，在世界观设定后、输出格式前
**注入内容**:
```
## 创意指数指导（创意指数 = 0.49）
叙事节奏：标准叙事，三幕式结构，常规节奏控制
镜头数量：根据内容适度分配，不刻意增减
台词风格：自然流畅，不过度戏剧化
...
## 视觉表现指导
镜头语言：标准镜头组合，常规运镜，不追求炫技
色彩设计：标准调色，真实色彩表现
...
```

#### 修复2：好莱坞导演技能集成
**修改文件**: `hyperreality-system/index.js`
**插入位置**: Layer 2（制作引擎）完成后、字段标准化前
**集成逻辑**:
```javascript
const { routeAndEnhance } = require('../skills/hollywood-cinematography/cinematography-skill-router');
const { enhancedShots, report } = routeAndEnhance(productionResult.shots, {
  minScore: 5,
  maxSkillsPerShot: 2
});
// 同步更新 shots 和 prompts
```
**匹配机制**:
- 优先级1: 类型+导演+情绪（最精确）
- 优先级2: 类型+导演
- 优先级3: 类型+情绪
- 优先级4: 类型匹配
- 特殊处理: 航拍/IMAX/雨夜标签加分

### 验证结果

#### 创意指数验证 ✅
- prompt 正确包含：`## 创意指数指导（创意指数 = 0.49）`
- LLM 现在能读取完整的创意指数配置
- 包含叙事节奏、镜头数量、台词风格、视觉表现等指导

#### 导演技能验证 ✅
- 测试3个镜头，全部成功匹配
- 镜头1: `剧情_卡梅隆_紧张` (手持跟拍)
- 镜头2: `剧情_卢卡斯_紧张` (手持特写)
- 镜头3: `科幻_诺兰_神秘` (科幻场景)
- 149个技能文件全部就绪

### 技术要点

1. **Skill Router API**:
   - `extractShotMetadata(shot)`: 从shot描述中提取类型/导演/情绪/镜头类型
   - `matchSkills(meta, limit)`: 根据元数据匹配技能，返回排序后的技能列表
   - `injectSkillEnhancement(shot, skills)`: 将技能注入shot的prompt
   - `routeAndEnhance(shots, options)`: 批量处理所有shots

2. **CreativeIntensityEngine API**:
   - `parseIntensity(input)`: 解析输入为0.0-1.0数值
   - `generateInstructions(intensity)`: 生成各Layer配置指令
   - 支持语义映射："保守"=0.2, "标准"=0.3, "轻度"=0.35等

### 版本号建议
- 超现实系统: v1.2.5 → v1.2.6（功能修复）
- 导演技能集成版本: v1.0
- 创意指数注入版本: v1.0

### 待办
- [ ] 完整链路端到端测试（从需求输入到最终prompt）
- [ ] 验证导演技能注入对渲染质量的实际影响
- [ ] 验证创意指数对不同值（0.2 vs 0.8）的差异化输出
- [ ] 考虑将导演技能匹配逻辑前置到剧本生成阶段（当前在制作阶段注入）

---

## 2026-06-20 17:12 - SC01 渲染状态

SC01预生产已完成（3镜头），因Seedance API对带reference_image的渲染较慢，采用仅1张front定妆照策略。

渲染任务状态检查因session超时未完成，需后续跟进。

---

## 2026-06-20 17:30 — EP02 第二集预生产完成（创意指数 0.98 + 导演技能修复后）

### 执行摘要
队长要求用最新版超现实系统跑第二集预生产，验证创意指数 0.98 和导演技能是否真正生效。

**修复前置**:
- `requirement-list-builder.js` 修复：优先读取 `metadata.creativeIntensity`，回退到文本正则匹配
- `script-generator.js` 修复：在 `_buildGenerationPrompt()` 注入 `## 创意指数指导`
- `hyperreality-system/index.js` 修复：在 ProductionEngine 后注入 `routeAndEnhance` 调用

### 预生产结果

**需求清单**:
- 类型: EDU（教育科普）
- 时长: 62s（完美匹配目标）
- 创意指数: 0.98（L5 极致，12/12 能力激活）
- 风格: REAL（写实纪实）
- 角色: 陈卓

**剧本引擎**:
- 6 场景（SC00-SC05）
- 1 角色（陈卓）
- 11 句台词
- 校验评分: 98/100（duration 100分）

**制作引擎**:
- 6 镜头，总时长精确匹配 62s
- 镜头时长: 10s + 12s + 12s + 12s + 10s + 6s
- Prompt 字符数: 1366 / 1448 / 1458 / 1494 / 1416 / 1318
- 全部包含完整时间轴（4-5 段/镜头）

**镜头质量观察（创意指数 0.98 效果）**:
- SC00: "晨光切割空间"、"三十五毫米低角固定"、"晨跑者虚化掠过"
- SC01: "手持踉跄推进"、"阴影如病灶蔓延"、"空气闷热感穿透画面"
- SC02: "八十五毫米长焦俯冲"、"金属棚顶光斑如瞄准器"、"电焊火花般光斑"
- SC03: "斑驳光斑如心电图"、"诊断书印章随机盖印"、"虚荣之镜"
- SC04: "金黄阳光烤暖冷青"、"长影如箭矢指向尽头"
- SC05: "半透明琥珀剪影"、"视觉牧歌"

### 发现的问题

#### 问题1: DirectorSkills 类型错误 ⚠️
```
DirectorSkills: (shot.camera || shot.cameraMovement || "").toLowerCase is not a function
```
- 根因：`shot.camera` 是对象 `{ shot_size, movement, angle, lens, speed }`，不是字符串
- 影响：导演技能未完全应用到镜头
- 状态：预生产流程继续完成，但技能注入不完整

#### 问题2: 进程 SIGKILL ⚠️
- Phase 2 并行 Agent（visual-language + audio-design + continuity-review）触发 SIGKILL
- 可能因 OOM 导致，需后续优化并行度或串行执行

### 输出文件
- `hyperreality-system/output/ep02-preproduction-1781947859969.json`（95KB）
- 包含完整 stages: requirementList, creativeIntensity(L5), scriptEngine, productionEngine
- 缺少 `directorSkills` 字段（因类型错误中断注入）

---

## 2026-06-20 17:45 — 定妆照渲染失败调查（InternalServiceError）

### 背景
队长收到扣费消息，询问是否已提交渲染。检查发现 `render-submit-record.json` 记录了一个历史提交：
- `taskId`: `cgt-20260620154838-85g66`
- `shotId`: `SC01`
- `submittedAt`: `2026-06-20T07:48:38.838Z`

### 调查结果

**代码审查**:
- `render-submitter-core.js` 的 `buildPayload` 方法：
  - 构造 `content` 数组：`[{type:'text'}, {type:'image_url', image_url:{url:'data:...'}, role:'reference_image'}]`
  - 每个镜头只传 1 张 front 定妆照（注释说明：避免数据量过大导致 Seedance 内部错误）
  - 文件路径：`characters/chen-zhuo/portraits/chen-zhuo-front.png`
  - Base64 编码正常，MIME type 为 image/png

**历史任务对比**:
- `cgt-20260613232057-9fs5h`（健康科普 S01，6月13日）：提交成功，使用了定妆照
- `cgt-20260521205520-dwdrn`（横纹肌溶解 S01，5月21日）：提交成功，使用了定妆照
- `cgt-20260620151956-jnc7b`（SC01 v4）：返回 InternalServiceError
- `cgt-20260620154838-85g66`（SC01 v5）：返回 InternalServiceError

**关键发现**:
- 5月21日和6月13日的任务**成功**使用了 reference_image
- 6月20日的两个任务**失败**（InternalServiceError）
- 这表明：
  1. reference_image 机制本身是正确的（历史成功证明）
  2. 6月20日的失败可能是 Seedance 服务端临时问题，或定妆照文件有变化
  3. 不是代码级缺陷（构造格式与历史成功任务一致）

### 队长指示
- 不进行空白对照渲染测试（避免浪费费用）
- 优先通过日志、代码和文档诊断
- 检查 Seedance 官方 API 文档中 reference_image 规范
- 如果客户端机制无异常，准备联系火山引擎技术支持

### 待跟进
- [ ] 检查 `chen-zhuo-front.png` 文件大小是否异常（历史成功时 vs 现在）
- [ ] 查阅 Seedance 2.0 官方文档 reference_image 要求
- [ ] 对比历史成功任务与失败任务的 payload 差异
- [ ] 若确认客户端无问题，向火山引擎提技术工单

---

## 2026-06-20 17:50 — 导演技能类型错误根因

### 问题
```
(shot.camera || shot.cameraMovement || "").toLowerCase is not a function
```

### 根因分析
在 `cinematography-skill-router.js` 的 `extractShotMetadata` 函数中：
```javascript
const cameraStr = (shot.camera || shot.cameraMovement || "").toLowerCase();
```

但 `shot.camera` 在生产引擎输出中是对象：
```json
{
  "shot_size": "wide",
  "movement": "dolly_in",
  "angle": "eye_level",
  "lens": "35mm",
  "speed": "slow"
}
```

对象是 truthy 的，所以 `shot.camera || ""` 返回对象，对象没有 `toLowerCase` 方法，导致报错。

### 修复建议
需要将 `shot.camera` 转换为字符串后再调用 `toLowerCase()`：
```javascript
const cameraStr = (typeof shot.camera === 'string' ? shot.camera : 
                   (shot.camera?.movement || shot.cameraMovement || "")).toLowerCase();
```

或直接从对象的 movement 字段提取：
```javascript
const cameraStr = (shot.camera?.movement || shot.cameraMovement || "").toLowerCase();
```

---

*记录结束。下一条待办：修复导演技能类型错误，重新验证 EP02 预生产。*


## 2026-06-20 17:55 — 渲染任务失败根本原因找到！

### 调查进展
队长要求深度排查 `cgt-20260620154838-85g66` 的 `InternalServiceError`。

### 关键对比分析

| 任务 | 日期 | 状态 | 角色 | 脚本 |
|------|------|------|------|------|
| `cgt-20260613232057-9fs5h` | 6月13日 | ✅ 成功 | chen-nurse | `submit-unified-render.js`（旧） |
| `cgt-20260521205520-dwdrn` | 5月21日 | ✅ 成功 | chen-zhuo | `submit-unified-render.js`（旧） |
| `cgt-20260620151956-jnc7b` | 6月20日 | ❌ InternalServiceError | chen-zhuo | `render-submitter-core.js`（新） |
| `cgt-20260620154838-85g66` | 6月20日 | ❌ InternalServiceError | chen-zhuo | `render-submitter-core.js`（新） |

### 代码格式对比

**旧脚本 (`submit-unified-render.js`)** 和 **新核心 (`render-submitter-core.js`)** 的 `reference_image` 构造格式**完全一致**：
```javascript
{
  type: 'image_url',
  image_url: { url: 'data:image/png;base64,xxxxx' },
  role: 'reference_image'
}
```

**关键差异**：旧脚本传4张全角度定妆照，新核心只传1张 front（注释：避免数据量过大导致 Seedance 内部错误）。

### 根本原因：MIME 类型与文件格式不匹配 🎯

**文件头检查**：
```bash
$ xxd /root/.openclaw/workspace/characters/chen-zhuo/portraits/chen-zhuo-front.png | head
00000000: ffd8 ffe0 0010 4a46 4946 0001 0101 0048  ......JFIF...H
```

- `ffd8 ffe0` → **这是 JPEG 格式文件头**
- 但文件扩展名是 `.png`
- `render-submitter-core.js` 根据扩展名设置 MIME 类型为 `image/png`

```javascript
// 当前代码（错误）
const mimeType = fullPath.endsWith('.png') ? 'image/png' : 'image/jpeg';
```

**Seedance API 解析过程**：
1. 收到 `data:image/png;base64,xxx` 
2. 尝试按 PNG 格式解析 JPEG 数据
3. 解析失败 → `InternalServiceError`

### 为什么6月13日 `chen-nurse` 成功？
- 同样存在 JPEG 伪装成 PNG 的问题（扩展名 `.png` 但文件头是 JPEG）
- 但6月13日使用的是旧脚本 `submit-unified-render.js`
- 旧脚本传4张全角度定妆照，可能 Seedance 对多图场景有不同的处理逻辑
- 或者旧脚本的 `reference_image` 顺序/结构有微妙差异

### 修复方案
修改 `render-submitter-core.js` 的 `buildPayload` 方法：
```javascript
// 用文件头检测真实格式，而非依赖扩展名
function detectMimeType(filePath) {
  const header = fs.readFileSync(filePath).slice(0, 4);
  if (header[0] === 0xFF && header[1] === 0xD8) return 'image/jpeg';  // JPEG
  if (header[0] === 0x89 && header[1] === 0x50) return 'image/png';   // PNG
  // fallback to extension
  return filePath.endsWith('.png') ? 'image/png' : 'image/jpeg';
}
```

### 验证建议
1. 检查所有 `.png` 定妆照文件的真实格式
2. 将伪装成 PNG 的 JPEG 文件转换为真实 PNG 格式
3. 修复 MIME 类型检测逻辑，同时做防御性编程

### 待办
- [ ] 修复 `render-submitter-core.js` MIME 类型检测
- [ ] 检查所有定妆照文件格式
- [ ] 重新提交 SC01 测试
- [ ] 若修复后成功，验证 EP02 全链路渲染

---

