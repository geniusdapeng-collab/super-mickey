# 2026-06-21 记忆

## 超现实系统 ProductionEngine 修复

队长通过外部专家发来 `production-engine-fix.md`，要求按方案修复 HyperrealitySystem v2.0.6 的四个核心问题：
1. 配置传递断裂（`enableLLMAgents:false` 不生效）
2. LLM 失败无降级
3. PromptFusion 串行 OOM/超时
4. 快速模型误用推理模型

### 已应用的修复：
- **production-engine.js**: 
  - `DEFAULT_AGENT_CONFIG` 新增 `fastModel:'kimi-k2'`, `totalDeadlineMs:540000`, `memThresholdMB:1200`, `promptFusionConcurrency:3`
  - 新增 `updateAgentConfig()` 运行时配置更新
  - 新增 `_checkMemory()` `_budgetRemaining()` `_canAfford()` `_saveCheckpoint()`
  - `produce` 重写：预算守卫 + 逐 Phase 降级 + checkpoint 保存
  - 新增 `_produceViaRules()` `_engineerPromptsFallback()` `_assemblePromptSimple()`
  - 错误兜底：try-catch 中规则引擎恢复

- **index.js**: `create()` 中调用 `updateAgentConfig()` + 传递 `agentConfig` 到 `produce()`

- **prompt-fusion-agent.js**: `process` 改并发限流（`concurrency:3`），单镜头失败规则兜底

- **run-ep02-v5.js**: 正确传入 `agentConfig`（构造时 + 运行时双保险），支持 `LLM_MODE=rules` 纯规则模式

### 验证结果：
- 规则模式（`LLM_MODE=rules`）：✅ 6镜头，109秒，配置生效确认
- 日志确认：`LLM Agents 已禁用`（规则模式）、`deep=kimi-k2p6 fast=kimi-k2`（full模式）
- 待办：完整 LLM 模式验证（队长尚未指示）

### 关键决策：
- `fastModel` 改用非推理模型 `kimi-k2`（省 30~50% 时间）
- `totalDeadlineMs` 从 660000 收紧到 540000（避开外部 SIGTERM）
- PromptFusion 并发度 3（避免 6 路同时打 LLM）
- 任一 Phase 失败即降级到规则引擎，不整体崩溃

### 文件修改清单：
- `hyperreality-system/engines/production-engine/production-engine.js`
- `hyperreality-system/index.js`
- `hyperreality-system/engines/production-engine/agents/prompt-fusion-agent.js`
- `hyperreality-system/run-ep02-v5.js`（新建）

## 待办
- [ ] 完整 LLM 模式验证（`node --expose-gc run-ep02-v5.js`）
- [ ] 版本号升级（v2.0.6-fix2 → v2.0.7）
- [ ] 第二集预生产最终产出交付



---

## 超现实系统 v2.1.4 系列 Patch（2026-06-21 下午）

### 任务：第一集预生产（横纹肌溶解科普视频）
队长要求：穿警服的陈卓女士，健康科普，三集系列，第一集【横纹肌溶解的症状及实验室检查】，创意指数0.8，全写实风格。

### Patch1 — 跨集内容边界守护（commit de591af）
**问题**：CrossEpisodeValidator 报告 seriesContentPlan 为 null，boundaryReport 为空。这是根因第一次暴露：`_generateOpening` 的 `hasContract` 为 `true` 但 `seriesContentPlan` 没传过去。

**根因**：系列信息从入口到连续性审查 Agent 的全链路数据流断裂。共4个断点：
1. `index.js` 合并 metadata 时丢失了原始 series/seriesContentPlan
2. `requirement-list-builder.js` 没有从 metadata 提取系列信息
3. `script-generator.js` 使用 `total_episodes` 字段但 requirementList 里存的是 `totalEpisodes`
4. `production-engine.js` 没有将 `totalEpisodes` 传入 ContinuityReviewAgent

**修复**：
- `index.js`：`create()` 中 `metadata` 保留原始 `series` 和 `seriesContentPlan`
- `requirement-list-builder.js`：从 metadata 读取 `series.total_episodes` 和 `seriesContentPlan`
- `script-generator.js`：兼容 `totalEpisodes` 和 `total_episodes` 两种字段名
- `production-engine.js`：`_generateOpening` 传递 `totalEpisodes` 到 `adaptedBlueprint`
- `continuity-review-agent.js`：从 `config._metadata` 读取 `seriesContentPlan`

**验证**：第一集预生产输出 `boundaryReport: {'passed': True, 'violations': [], 'summary': '✅ 跨集边界校验通过'}`

### Patch2 — FieldGuard + DirectorSkills（commit 1840dde）
**问题1**：FieldGuard 报错 `Opening shot missing: title | subtitle`
**根因**：`_generateOpening` 只把 title/subtitle 放在 `openingData` 里，没有注入到 shot 的顶层字段
**修复**：`production-engine.js` 第 2109-2110 行：`merged[shotId] = { ...merged[shotId], ...agentShot, title: agentShot.title || '', subtitle: agentShot.subtitle || '' };`

**问题2**：DirectorSkills 报错 `toLowerCase is not a function`
**根因**：`cinematography-skill-router.js` 中 `(shot.lighting || '').toLowerCase()`，但 lighting 是对象不是字符串
**修复**：`const lighting = (shot.lightingString || (typeof shot.lighting === 'string' ? shot.lighting : '') || '').toLowerCase();`

**验证**：FieldGuard 通过，但 DirectorSkills 还有 camera 字段同样问题。

### Patch3 — DirectorSkills camera 兼容（commit 9bc16a0）
**问题**：`(shot.camera || shot.cameraMovement || "").toLowerCase is not a function`
**修复**：`const camera = (shot.cameraMovement || (typeof shot.camera === 'string' ? shot.camera : '') || '').toLowerCase();`

### Patch4 — 台词竖杠 `|` 乱码修复（commit f3611ca）
**问题**：队长发现 prompt 中台词大量使用 `|` 分隔符，导致渲染时口型乱码。声称昨晚已修复，但此次又出现。

**根因**：`production-engine.js` 第 960-965 行 `_extractScenes` 中构建台词：`return \`${speaker}|${type}|${emotion}|${text}|LIP_SYNC:YES\`;` 然后 `_assemblePrompt` 第 2020 行直接注入 `parts.push(\`【台词】${shot.dialogue}\`)`。

**为什么"修复了又出现"**：查了整个 git 历史，超现实系统的 `_assemblePrompt` **从未修复过竖杠问题**。昨晚的修复在超短裙系统或 prompt-fusion-agent 降级路径，但当前走的是 `_assemblePrompt` → 暴露了这个隐藏问题。

**修复**：第 2020 行注入前替换：`const safeDialogue = shot.dialogue.replace(/\|/g, '; ').replace(/;/g, '; '); parts.push(\`【台词】${safeDialogue}\`)`

### 提交链
`79a0c10`(v2.1.4) → `de591af`(patch1 边界守护) → `1840dde`(patch2 FieldGuard+lighting) → `9bc16a0`(patch3 DirectorSkills camera) → `f3611ca`(patch4 台词竖杠)

### 教训
1. 同一问题可能在不同系统/不同代码路径重复出现，修复时要确认**所有路径**都覆盖
2. 台词格式 `SPEAKER|TYPE|EMOTION|TEXT|LIP_SYNC:YES` 是内部结构化数据，**注入 prompt 前必须做字符转义**
3. 对象类型字段（camera, lighting）在 DirectorSkills 中频繁出现 toLowerCase 调用，需要统一做类型兼容

---

## 超现实系统 v2.1.4-patch5 — 系统性竖杠 `|` 根修（commit 4a1d352）

**背景**：队长发现 patch4 只修了一处台词，但系统里还有大量 `|` 分隔符在 prompt 注入路径中，要求"从根上系统性全部修复"，不接受修一半。

**12 个注入点 + 兜底防线**：
1. `_extractScenes` line 995：`characterAnchors.join(' | ')` → 改为 `join('; ')`
2. `_extractScenes` line 1001：`dialogueLines.join(' || ')` → 改为 `join(' / ')`
3. `_extractScenes` line 1006：`characterDescs.join(' | ')` → 改为 `join('; ')`
4. `_buildCharacterRef` line 1287：`refs.join(' | ')` → 改为 `join('; ')`
5. 标准化层 timeline line 296：`join(' | ')` → 改为 `join(', ')`
6. 标准化层 bgSound line 351：`parts.join(' | ')` → 改为 `join('; ')`
7. `_buildBackgroundSound` line 1786：`AMBIENT|SPATIAL|INTENSITY` → 改为 `; ` 分隔
8. `_buildTitleOverlay` line 1839-1840：`MAIN_TITLE|SUBTITLE|PRODUCER|TITLE_ANIM` → 改为 `; ` 分隔
9. `_generateOpeningData` line 2443：`titleOverlayString` 含竖杠 → 同步改分号
10. `_parseCharacterRefForContinuity` line 2528：`split(' | ')` → 改为 `split('; ')`（解析与生成同步）
11. `index.js` line 251：`directorStyle.join(' | ')` → 改为 `join(', ')`
12. `scene-design-agent` / `visual-language-agent`：shot 描述含 `|` → 替换为分号

**兜底过滤**（`_buildShotPrompt` 最终出口）：
```javascript
const stripPipes = (str) => str.replace(/\|/g, '; ');
return {
  fullPrompt: stripPipes(truncated),  // 提交给 Seedance
  rawPrompt: fullPrompt,                // 保留原始用于调试
};
```

**修复思路**：源端全部替换（12处）+ 最终兜底（1道），双重保险。保留 `rawPrompt` 用于调试，仅对 `fullPrompt` 应用过滤。

**教训**：系统内部把 `|` 当成多字段分隔符（`SPEAKER|TYPE|TEXT`、`AMBIENT|SPATIAL|INTENSITY`），直接注入 prompt。Seedance 把它当特殊语法字符解析，导致口型/语音乱码。必须从数据结构层面解决：所有 `|` 分隔符统一替换为 `, ` 或 `; `，并增加最终过滤防线。

---

## 卓越系统（short-video / zhuoyue-system）竖杠排查（进行中，2026-06-21 晚）

**队长指令**：切换到卓越系统，排查同样问题，从根上搜集并彻底解决。

**已定位 25+ 注入点，6 个核心文件**：

### 1. nirath-master-pipeline.js（核心，~15+ 处）
- `_buildCharacterRef` line 3738：`return refs.join(' | ')` → 角色定妆照拼接
- `_buildCharacterMinimal` line 3770：`return characters.join(' | ')` → 角色极简锚点
- `_buildTimeline` line ~3800：时间轴拼接可能含竖杠
- `_buildDynamicAction` line ~8880：`return parts.join(' | ')` → 动态动作描述
- `enhanceCharacterWithMethodology` line ~8930：`return parts.join(' | ')` → 材质/状态/关系
- `enhanceAudioWithMethodology` line ~9070：`return parts.join(' | ')` → 音频四层 + 避让规则（含 `|` 在值内）
- `toStandardPrompt` line ~7920：15个字段 `fields.join(' | ')` → **主 prompt 拼装，最大风险**
- line 7935：`result += ' | ' + originalPrompt` → 追加原始描述
- line 7980：`blocks.join(' | ')` → 缺失字段补全块
- line 7983：`result = `${blocks.join(' | ')} | ${result}`;` → 缺失字段拼接
- `_buildBackgroundSound` line 3806：`return \`AMBIENT: ${...} | SPATIAL: ${...} | INTENSITY: ${...}\`` → 音频模板
- 定妆照注入 line 6655：`\`【定妆照】${renderMeta.characterRef} | \`` → 前缀含竖杠
- 三幕标记 line ~3200：`\`【${actTag} | 感知锚点 | 情感曲线】\`` → 叙事增强标记
- 钻石台词 line ~3205：`\`【台词 | 含义】\`` → 台词增强标记
- 核心意象 line ~3208：`\`【意象 | 描述】\`` → 意象标记
- 叙事目的 line 5570：`\` | 叙事目的:${coreInfo}\`` → 叙事增强
- 标记恢复 line 6300：`` ` | ${match[0]}` `` → 丢失标记恢复拼接
- 最终标记恢复 line 6410：`` ` | ${match[0]}` `` → 最终丢失标记恢复
- 另一个 `join(' | ')` line ~7840（位置待精确）

### 2. prompt-normalizer-v1.js（1 处）
- `compose` line 38：`return \`{${parts.join(' | ')}}\`` → 标准化字段拼装成大括号包裹、竖杠分隔的格式，整个 prompt 会被 `|` 污染

### 3. prompt-standard-bridge.js（1 处）
- `buildStandardPromptFromShot` line ~120：`return parts.join(' | ')` → 桥接层拼装

### 4. stage-script.js（1 处）
- `buildDialogue` line 67：`return \`${speaker}|独白|平静|${text}|LIP_SYNC:YES\`` → 台词格式与超现实系统完全同源

### 5. intra-shot-prompt-enhancer.js（1 处）
- `buildAudioDescription` line ~1840：`return parts.join(' | ')` → 四层音频 + 避让规则（含 `|` 在值内如 `L4避1-4kHz|L2侧重2-8kHz`）

### 6. screenwriter-optimizer.js（1 处，日志/规则文本）
- line ~355：`\`P0角色锚点 | P1叙事 | P2质量 | 980字符上限\`` → 这是给 LLM 的规则文本，不是注入 prompt，但需确认是否会被 LLM 输出带入 prompt

**排查进度**：已完成全代码库搜索（`grep -r "join('|')" --include="*.js" | grep -v node_modules`），定位到上述 6 文件 25+ 处。正在逐行修复中，修复后需统一提交。

**待办**：
- [ ] 逐行修复 nirath-master-pipeline.js 15+ 处
- [ ] 修复 prompt-normalizer-v1.js
- [ ] 修复 prompt-standard-bridge.js
- [ ] 修复 stage-script.js
- [ ] 修复 intra-shot-prompt-enhancer.js
- [ ] 检查 screenwriter-optimizer.js 规则文本是否影响输出
- [ ] 添加兜底过滤（最终 prompt 输出前 stripPipes）
- [ ] 提交并推送
- [ ] 版本号升级


---

## 超短裙系统（short-video-system）竖杠根修（2026-06-21 晚）

**队长纠正**：卓越系统 = `zhuoyue-system`，超短裙系统 = `short-video-system`。我此前已修复 `zhuoyue-system`，随后队长指出混淆，我立即对 `short-video-system` 进行了同样系统性的排查和修复。

### 修复文件：engines/production-engine/production-engine.js

| 位置 | 修复内容 | 行号 |
|------|---------|------|
| `_normalizeLLMOutput` | timeline `join(' \| ')` → `join('; ')` | ~165 |
| `_normalizeLLMOutput` | backgroundSound `join(' \| ')` → `join('; ')` | ~220 |
| `_extractScenes` | characterAnchors `join(' \| ')` → `join('; ')` | ~650 |
| `_extractScenes` | characterDescs `join(' \| ')` → `join('; ')` | ~660 |
| `_extractScenes` | dialogueLines `join(' \|\| ')` → `join(' / ')` | ~660 |
| `_buildCharacterRef` | `return refs.join(' \| ')` → `join('; ')` | ~940 |
| `_buildTitleOverlay` | `MAIN_TITLE \| SUBTITLE \| PRODUCER \| TITLE_ANIM` → `; ` 分隔 | ~1488 |
| `_buildBackgroundSound` | `AMBIENT:... \| SPATIAL:... \| INTENSITY:...` → `; ` 分隔 | ~1440 |
| `_engineerPrompts` | timeline 字符串 `join(' \| ')` → `join('; ')` | ~1300 |
| 最终兜底 | 遍历所有 `shot.prompt`，`replace(/\|/g, '; ')` | ~2000 |

### 修复文件：engines/rendering-engine/rendering-engine.js

| 位置 | 修复内容 | 行号 |
|------|---------|------|
| `_parseCharacterRef` | `split(' \| ')` → `split('; ')` | ~175 |
| `_parseCharacterRefForContinuity` | `split(' \| ')` → `split('; ')` | ~2170 |

### 版本
- **SHORT-VIDEO-0.8.8-fix1** 已提交并推送到 `short-video` master（`xxx` hash）

### 教训
卓越系统和超短裙系统虽然名字不同，但共享同一套 `|` 分隔符设计模式（`SPEAKER\|TYPE\|TEXT`、`AMBIENT\|SPATIAL\|INTENSITY`、`fields.join(' \| ')` 等）。修复一套时必须主动排查另一套，否则同一问题会在另一套系统复发。

