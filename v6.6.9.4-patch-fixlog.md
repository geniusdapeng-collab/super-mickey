# v6.6.9.4 修复日志（patch21-27）

## 预生产验证结果

| 项目 | 数值 |
|------|------|
| 状态 | ✅ 成功完成 |
| 质量分数 | 76分 | 等级A | 通过 |
| 总耗时 | 222秒（3.7分钟） |
| 镜头数 | 6个 |
| 视频时长 | 64秒 |
| Nirath残留 | **0处** |

## Patch 变更记录

### patch21: 修复AmbientSoundDesigner构造函数错误
- **问题**：`AmbientSoundDesigner is not a constructor`
- **根因**：`ambient-sound-designer.js`已重构为纯函数式API，但`ambient-sound-designer.bridge.js`和`subsystem-orchestrator-v1.js`仍使用`new AmbientSoundDesigner()`
- **修复**：
  - `ambient-sound-designer.bridge.js`：改为函数式调用`generateAmbientSoundField()`
  - `subsystem-orchestrator-v1.js`：改为函数式调用，添加`mode`参数

### patch22: 修复nirath目录内模块相对引用路径
- **问题**：`Cannot find module './llm-reasoning-engine'`
- **根因**：移动Nirath模块到`systems/nirath/`后，相对路径未更新
- **修复**：批量修复5个文件的require路径（creative-llm-router-v1.js、director-final-review.js等）

### patch23: 修复ShotDebugRecorder缺少getRecord方法
- **问题**：`this.debugRecorder.getRecord is not a function`
- **根因**：`shot-debug-recorder-v1.js`只有`append()`方法，没有`getRecord()`
- **修复**：添加`getRecord(shotId)`方法，支持读取已记录镜头数据

### patch24: 修复SubsystemOrchestratorV2缺少DIALOGUE字段
- **问题**：`缺少必填字段: DIALOGUE`
- **根因**：`subsystem-orchestrator-v2.js`的`baseFields`未包含DIALOGUE
- **修复**：添加DIALOGUE字段，从`shot.dialogue || shot.narration`提取

### patch25: FinalPromptBuilderV3 generic模式隔离
- **问题**：generic模式调用Nirath专属CreativeLLMRouter，导致LLM输出带神话残留
- **根因**：`final-prompt-builder-v3.js`在`_shouldUseLLM()`中未判断模式
- **修复**：
  - 构造函数接收`mode`参数
  - `_shouldUseLLM()`在generic模式返回`false`
  - `creative-llm-router-v1.js`默认style从'神话电影感'改为'电影级质感'

### patch26: 修复FinalPromptBuilderV3返回结构缺失success字段
- **问题**：`新链路异常: 原因:未知`
- **根因**：`final-prompt-builder-v3.js`返回对象没有`success`字段，`nirath-master-pipeline.js`检查`newChainResult.success`为undefined
- **修复**：返回对象添加`success: true`

### patch27: 清理camera-movement-system和micro-expression-system中山海经残留
- **问题**：通用模块注释/默认值含山海经字样
- **修复**：
  - `camera-movement-system-v3.js`：注释去山海经化，默认protagonistName '小G'→'主角'
  - `micro-expression-system-v2.js`：注释去神兽化

## 队长提出的3个核心问题及进展

### 问题1：LLM降级改报错 → 避免报错
- ✅ 已收集4个根因并修复（见上表）
- 🔄 待建立：问题收集日志→根因分析→自动修复/外部专家机制

### 问题2：模块路由机制
- 🔄 已建物理隔离层（目录隔离+懒加载+模式判断）
- 🔄 待建：ModuleRouterV1（统一入口+版本路由+错误拦截）
- 设计草稿：`/root/.openclaw/workspace/module-router-v1-draft.md`

### 问题3：Promptforge残留
- ✅ patch25已隔离：generic模式不调用CreativeLLMRouter
- ✅ `promptforge-director-worker.js`当前LOCAL_MODE=true，使用本地合成，无LLM调用
- ⚠️ 发现隐患：`mode`参数传入worker但未被使用，建议添加模式判断增强安全性

## 下一步待办

1. **等待队长确认**：清理范围（仅generic输出 vs 全代码库去Nirath化）
2. **ModuleRouterV1**：根据队长反馈实施模块路由机制
3. **PromptForge模式隔离**：修复worker中mode参数未使用问题
4. **持续清理**：根据队长指示处理剩余残留
