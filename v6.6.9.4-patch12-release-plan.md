# 🎬 v6.6.9.4-patch12 成果规划

> **版本**: v6.6.9.4-patch12
> **发布时间**: 2026-06-17
> **GitHub**: `aac5e9d` @ hyperreal master

---

## 一、版本变更摘要

| 项 | 内容 |
|----|------|
| 前置版本 | v6.6.9.4-patch11 |
| 核心主题 | 人物灵动感软性提升 |
| 代码修改量 | 零行系统代码（纯软性注入） |
| 修改文件 | `run-preproduction-v664.js` + `characters/chen-zhuo/character-card.json` |

---

## 二、人物灵动感提升方案（5层软性注入）

### 第1层：任务描述注入 ✅ 已落地

**文件**: `run-preproduction-v664.js`

新增字段：
```javascript
directorNote: '陈卓表现要活泼灵动，像朋友一样亲切，拒绝呆板说教感',
performanceStyle: 'vlog-style-interactive',
mannerisms: [
  '说话时习惯用手比划强调重点',
  '讲到关键信息会身体前倾靠近镜头',
  '偶尔从画面侧方突然进入镜头制造惊喜',
  '允许快速走向镜头形成怼脸特写',
  '表情丰富，会挑眉、歪头、微笑',
  '不喜欢呆站在原地，会自然走动、转身'
],
cameraStyle: 'dynamic-vlog',
```

**生效链路**:
- Stage-5（剧本生成）→ LLM 吸收性格设定，生成活泼台词
- Stage-5B（visualPrompt）→ LLM 转化为动态视觉描述
- Stage-9（运镜v4）→ LLM 生成更具动感的镜头时间轴
- Stage-11（渲染核心）→ PromptTier 注入动态导演指令

### 第2层：角色卡片升级 ✅ 已落地

**文件**: `characters/chen-zhuo/character-card.json`

```json
"personality": {
  "core": "warm",
  "traits": ["kind", "brave", "playful", "energetic", "approachable"],
  "mbti": "ENFP",
  "mannerisms": [
    "说话时习惯用手比划",
    "偶尔会歪头微笑",
    "讲到重点时会身体前倾靠近镜头"
  ],
  "performanceStyle": "vlog-style-interactive",
  "energyLevel": "high"
}
```

**生效链路**:
- Stage-4（角色系统）→ CharacterPromptBuilder 将 mannerisms 融入每个镜头 Prompt
- Stage-11（渲染核心）→ 角色身份描述携带 personality 特征

### 第3层：运镜风格注入 ✅ 已落地

通过 `cameraStyle: 'dynamic-vlog'` 传递至 Stage-9 运镜系统。

**预期效果**:
- 更多 push-in（镜头推进）和 pull-out（镜头拉远）
- 角色进出画面的动态调度
- 手持感、即兴感的运镜时间轴

### 第4层：风格标签切换 ✅ 已落地

入口脚本中 `performanceStyle: 'vlog-style-interactive'` 影响 Stage-11 PromptTier 导演注入。

**预期效果**:
- 更近距离的景别选择
- 更具互动感的构图
- 更随意的镜头语言

### 第5层：创意指数已拉满 ✅ 已有

v669 中 `creativeIndex: 1.0` 已激活：
- 运镜系统v4（LLM驱动个性化）
- PromptForge 自由编排
- 情绪增强模块

---

## 三、关键认知：为什么零代码就能实现

队长要求的三种表现形式，本质都是 **Prompt 层面的空间/动作描述变化**：

| 表现形式 | Prompt 层面的转化 |
|----------|------------------|
| 脸怼镜头 | `中景半身` → `极近距离特写，人物面部充满画面，眼神直视镜头` |
| 突然冒出 | `人物站立画面中央` → `人物从画面下方突然进入，上半身先入镜，制造惊喜感` |
| 活泼不呆板 | `平静站立` → `身体微微前倾，单手比划，边走边说，自然转身` |

LLM 在 Stage-5B（visualPrompt）和 Stage-9（运镜）中，只要上游把"活泼好动"的意图传达到位，就会自动产出对应的动态描述。

**不需要修改任何系统模块代码。**

---

## 四、验证计划

### 方案A：快速验证（推荐）
1. 使用 patch12 配置直接跑预生产
2. 对比 patch11 vs patch12 的 visualPrompt 差异
3. 重点检查：
   - 是否有"怼脸""冒出"相关的空间描述
   - 运镜时间轴是否更动态（更多 push-in / orbit / sudden movement）
   - 角色动作描述是否更活泼（手势、走动、转身）

### 方案B：对照实验
1. 跑 patch12 预生产（启用灵动感注入）
2. 跑一个对照组（关闭灵动感注入，其他参数相同）
3. 对比两组输出的 visualPrompt 和 cameraMovement

---

## 五、风险与兜底

| 风险 | 概率 | 应对方案 |
|------|------|----------|
| LLM 未充分吸收 mannerisms | 中 | 在任务描述中重复强化指令，或提高 creativeIndex |
| 动态描述导致 Seedance 渲染不稳定 | 低 | Seedance 2.0 对动态场景支持较好，如不稳定可回退到保守描述 |
| 过于活泼影响科普专业度 | 低 | 内容防火墙已锁定医学事实，仅表现层受影响 |
| 怼脸镜头导致面部变形 | 低 | 定妆照参考 + 负面约束（no distorted face）兜底 |

---

## 六、下一步行动

**队长决策点：**

1. **立即验证** → 用 patch12 配置跑一次预生产，对比 patch11 输出，确认灵动感提升效果
2. **直接渲染** → 如果队长对 patch11 的 Prompt 内容已满意，可直接用 patch11 提交渲染，patch12 作为下一集优化
3. **对照实验** → 跑两组（有/无灵动感注入）对比后再决定

**推荐路径：** 方案A 快速验证 → 确认效果 → 提交渲染

---

## 七、附录：修改文件清单

| 文件 | 修改内容 | 类型 |
|------|----------|------|
| `run-preproduction-v664.js` | 新增 directorNote / performanceStyle / mannerisms / cameraStyle | 软性注入 |
| `characters/chen-zhuo/character-card.json` | 升级 personality 字段（mannerisms + performanceStyle + energyLevel） | 角色卡片 |
| `.current-version` | v6.6.9.4-patch11 → patch12 | 版本号 |

---

> **核心原则**：系统升级优先。本次人物灵动感提升完全通过软性注入实现，未修改任何系统模块代码，确保通用性和可复用性。
