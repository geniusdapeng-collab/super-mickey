# P0-2 软性剥离修改记录

## 日期
2026-06-16

## 目标
解决 Generic 模式输出含 Nirath 元素的问题（P0-1 修复不完全），通过软性剥离隔离 Nirath 专属逻辑。

## 修改原则
- P0-2 软性剥离：不移动文件，通过模式判断隔离 Nirath 专属逻辑
- 所有修改服务通用系统，不单 case 定制

---

## 修改详情

### 1. opening-system-v3.js ✅
**状态**: 无需修改（v6.5.64 已有 Generic 模式支持）
- 已有 `generateGenericOpening` 函数
- 已有 `isGenericVideo` 模式检测

### 2. aetherium-material-engine.js ✅
**修改**: `generateNirathMaterialSection` 函数
- 添加 `isNirath` 参数（第 345 行）
- Generic 模式不注入 Nirath 大气介质
- Nirath 模式自动追加大气介质描述

### 3. beast-opening-line-agent.js ✅
**修改**: `generateOpeningLine` 方法
- habitat 默认保持 'Nirath' 但可被覆盖
- 支持 `config.habitat` 参数

### 4. astralis-camera-engine.js ✅
**修改**: `compressCameraForPromptSpace` 函数
- 添加 `isNirath = true` 参数（第 366 行）
- Generic 模式使用 `GENERIC_CAMERA_MOVEMENTS`
- Nirath 模式使用 `NIRATH_CAMERA_MOVEMENTS`

### 5. nirath-scene-mapper.js ✅
**状态**: 无需修改
- 已在 `nirath-master-pipeline.js` 中被 `if (this.mode === 'nirath')` 包裹
- Generic 模式不调用

### 6. story-craft-engine ✅
**修改**: `nirath-master-pipeline.js` 第 1796 行
- 添加 Generic 模式检查：`if (this.mode !== 'nirath')`
- Generic 模式跳过 StoryCraft，fallback 到 LLM 生成剧本

---

## 系统排查额外修复

### 7. nirath-master-pipeline.js 内部硬编码

#### 7.1 generateDefaultVisual 方法
- Generic 模式输出「场景中」而非「Nirath异世界场景中」
- Generic 模式输出「环境展示,突出场景独特氛围」而非「Nirath独特生态」

#### 7.2 片头 dialogue
- Generic 模式输出「系列片头」而非「山海经系列片头」

#### 7.3 protagonistProfile
- 使用 `input.protagonistId` 而非硬编码 `xiaoG`
- 支持 Generic 模式自定义主角

#### 7.4 微动作/异兽动作增强
- Generic 模式跳过 Nirath 专属动作增强
- 避免注入「超写实科幻」等 Nirath 风格

#### 7.5 world.name fallback
- 所有 prompt/schema 中的 fallback 从「Nirath」改为「现实世界」
- 影响位置：第 2018、2215、2299、2317 行

### 8. promptforge-director-worker.js ✅

#### 8.1 输入数据
- 从 `nirath-master-pipeline.js` 传递 `mode` 参数
- `inputData.mode = this.mode || 'nirath'`

#### 8.2 buildRewritePrompt 函数
- 添加 `mode = 'nirath'` 参数
- Generic 模式构建通用纪录片风格 Prompt：
  - 无「山海经」项目名
  - 无「双恒星」光照
  - 无「Nirath 特征」
  - 无「磁场」「低重力」等科幻元素

---

## 测试建议

1. **Generic 模式测试**：
   - 输入：真实世界纪录片/科普视频需求
   - 验证：输出不含 Nirath/异兽/山海经元素
   - 检查：Prompt 中无「双恒星」「磁场」「低重力」

2. **Nirath 模式回归测试**：
   - 输入：山海经系列视频需求
   - 验证：输出仍包含 Nirath 专属元素
   - 检查：片头、五要素、异兽动作等正常

---

## 版本号
建议升级至 **v6.6.9.1**（P0-2 软性剥离补丁）
