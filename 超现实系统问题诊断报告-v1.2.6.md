# 超现实系统全面问题诊断报告

> 生成时间: 2026-06-19 15:20  
> 版本: v1.2.6  
> 报告目的: 供外部专家分析问题根因与解决方案  

---

## 目录

1. [系统概述](#1-系统概述)
2. [已知问题汇总](#2-已知问题汇总)
3. [详细问题分析](#3-详细问题分析)
4. [关键代码片段](#4-关键代码片段)
5. [错误日志与报错](#5-错误日志与报错)
6. [期望结果](#6-期望结果)
7. [附录：版本历史](#7-附录版本历史)

---

## 1. 系统概述

### 1.1 系统定位

**超现实工业创作系统（Hyperreality System）** 是一个面向工业级 AI 视频创作的完整解决方案，支持从创意意图到成片交付的全链路自动化。

### 1.2 架构分层

```
Layer 0: 需求清单生成与确认
Layer 1: 剧本引擎 (ScriptEngine) - 生成结构化剧本
Layer 2: 制作引擎 (ProductionEngine) - 生成镜头提示词
Layer 3: 渲染引擎 (RenderingEngine) - 提交 Seedance API
Layer 4: 后期引擎 (PostProductionEngine) - 字幕/音乐/弹幕
```

### 1.3 核心文件

| 文件 | 职责 | 大小 |
|------|------|------|
| `index.js` | 系统统一入口，编排四层架构 | 740 lines |
| `engines/production-engine/production-engine.js` | 镜头提示词生成 | 1,792 lines |
| `engines/script-engine/core/script-generator.js` | LLM剧本生成 | ~400 lines |
| `engines/field-guard.js` | 字段校验守门器 | ~120 lines |
| `engines/field-standardizer.js` | 字段标准化器 | ~350 lines |

### 1.4 当前版本

- **版本号**: v1.2.6
- **Git**: `a0a679e` (master分支)
- **仓库**: github.com:geniusdapeng-collab/hyperreal.git

---

## 2. 已知问题汇总

### 2.1 问题分类矩阵

| 优先级 | 问题 | 影响 | 状态 |
|--------|------|------|------|
| P0 | 质量门字段检查与v6.37标准不匹配 | 预生产全部失败 | ✅ 已修复(v1.2.6) |
| P0 | 角色覆盖机制失效（生成小G/小R而非陈卓） | 角色错误 | 🔧 待验证 |
| P0 | `timing`对象已从标准输出移除但代码仍依赖 | 时长分配逻辑 | 🔧 待修复 |
| P1 | 时长显示 `_generateFinalReport` 使用已移除字段 | 报告显示0s | ✅ 已修复(v1.2.6) |
| P1 | FieldGuard检查已移除字段 `portraits`/`characterCards` | 误报警告 | ✅ 已修复(v1.2.6) |
| P1 | 预生产脚本未传递 `characters` 数组到metadata | 角色覆盖未触发 | 🔧 待验证 |
| P2 | LLM temperature参数非法（非1值被拒） | Stage 5B优化失败 | 🔧 待修复 |
| P2 | Markdown报告生成异常（仅输出"TODO"） | 报告不完整 | 🔧 待修复 |
| P2 | PromptForge批次提取失败（content长度0） | Prompt质量降级 | 🔧 待修复 |
| P2 | 空内容/推理内容分离问题 | LLM返回异常 | 🔧 待修复 |
| P3 | 山海经IP硬编码残留 | 系统通用性 | 🔧 持续清理 |
| P3 | JSON截断解析脆弱性 | 数据解析 | 🔧 需加固 |

---

## 3. 详细问题分析

### 3.1 问题1: 质量门字段检查与v6.37标准不匹配 (P0)

#### 背景

v6.37标准将 `lighting`、`timeline`、`backgroundSound` 等字段从嵌套对象改为**独立字符串字段**（如 `lightingString`、`timelineString`、`backgroundSoundString`），但质量门检查逻辑仍使用旧的对象访问方式。

#### 当前代码 (修复前)

```javascript
// engines/production-engine/production-engine.js:1503-1508
hasLighting: !!(p.lighting?.string || p.lighting) && (p.lighting?.string || p.lighting).toString().includes('K'),
hasTimeline: !!(p.timeline?.string || p.timeline) && (p.timeline?.string || p.timeline).toString().includes('T00:'),
hasBackgroundSound: !!(p.backgroundSound?.string || p.backgroundSound) && (p.backgroundSound?.string || p.backgroundSound).toString().includes('AMBIENT:'),
```

#### 实际标准输出字段

```javascript
// v6.37标准输出（_engineerPrompts返回）
{
  shotId: 'S01',
  duration: 10,
  scene: '医院走廊',
  mood: '专业',
  camera: { object: {...}, string: 'medium shot' },
  cameraString: 'medium shot',
  lighting: { object: {...}, string: 'front light' },
  lightingString: 'front light',
  characterRef: '陈卓-警服',
  character: '穿警服的陈卓',
  action: '站立讲解',
  dialogue: '陈卓|独白|专业|...',
  timeline: { object: {...}, string: 'T00:00-T00:10' },
  timelineString: 'T00:00-T00:10',
  backgroundSound: { object: {...}, string: 'AMBIENT: ...' },
  backgroundSoundString: 'AMBIENT: ...',
  prompt: '完整提示词...',
  promptCharCount: 1234
}
```

#### 报错结果

```
质量门: 失败 ✗ (0/6)
每个镜头缺失: hasLighting, hasTimeline, hasBackgroundSound
```

#### 根因

质量门检查访问的是 `p.lighting?.string`，但标准输出中的 `lighting` 是对象，`lightingString` 才是字符串。检查逻辑与输出结构不匹配。

#### 修复 (v1.2.6)

```javascript
hasLighting: !!(p.lightingString) && p.lightingString.includes('K'),
hasTimeline: !!(p.timelineString) && p.timelineString.includes('T00:'),
hasBackgroundSound: !!(p.backgroundSoundString) && p.backgroundSoundString.includes('AMBIENT:'),
```

---

### 3.2 问题2: `timing`对象已从标准输出移除但代码仍依赖 (P0)

#### 背景

v6.37标准将 `timing: {start, duration, end}` 对象从标准输出中移除，改为直接使用 `duration` 字段。但生产引擎内部大量逻辑仍依赖 `shot.timing`。

#### 受影响代码位置

```javascript
// production-engine.js:420-442 (时长分配)
const origDuration = shot.timing?.duration || 10;
shot.timing = {
  start: startTime,
  duration: allocatedDuration,
  end: startTime + allocatedDuration
};

// production-engine.js:438 (时长微调)
const diff = targetDuration - lastShot.timing.end;
lastShot.timing.duration += diff;

// production-engine.js:559 (时间轴构建)
const baseDuration = shot.timing.duration;

// production-engine.js:1776 (总时长统计)
totalDuration: result.shots.reduce((sum, s) => sum + s.timing.duration, 0),
```

#### 期望行为

内部逻辑可以继续使用 `shot.timing` 进行计算，但**标准输出**（`_engineerPrompts` 返回）不应包含 `timing` 字段。

#### 当前状态

- 内部计算: 使用 `shot.timing` ✅ 正常
- 标准输出: 不包含 `timing` ✅ 正常
- 但 `index.js` 的 `_generateFinalReport` 曾错误访问 `shot.timing.duration` → 已修复(v1.2.6)

---

### 3.3 问题3: 角色覆盖机制失效 (P0)

#### 背景

系统要求：当用户指定了角色（如"穿警服的陈卓女士"），LLM生成的剧本必须使用指定角色，不能自创角色（如"小G"、"小R"）。

#### 角色覆盖代码

```javascript
// engines/script-engine/core/script-generator.js:374-424
_parseLLMResponse(response, userIntent) {
  // ... JSON解析 ...
  
  // v1.2.5: 从metadata注入角色信息（覆盖LLM生成的错误角色）
  const metadataChars = userIntent.metadata?.characters || [];
  if (metadataChars.length > 0) {
    const overrideCharacters = metadataChars.map(c => ({
      character_id: c.id || c.name,
      name: c.name,
      // ...
    }));
    
    // 替换LLM生成的角色
    parsed.character_system = { characters: overrideCharacters };
    
    // 替换所有场景中的角色引用
    if (parsed.structure?.scenes) {
      for (const scene of parsed.structure.scenes) {
        if (scene.characters) {
          scene.characters = overrideCharacters.map(c => c.character_id);
        }
        if (scene.dialogue?.lines) {
          for (const line of scene.dialogue.lines) {
            line.speaker = overrideCharacters[0]?.name || line.speaker;
          }
        }
      }
    }
  }
}
```

#### 问题现象

第二集预生产生成了小G(医生)和小R(患者)的双人对话，而非陈卓单人讲解。

#### 根因分析

1. **预生产脚本未传递 `characters` 数组**
   ```javascript
   // run.js (修复前)
   { title: '第二集...', target_duration: 62, series: '...', episode: 2 }
   // 缺少: characters: [{ name: '陈卓', description: '...' }]
   ```

2. **即使传递了characters，覆盖逻辑可能未完全覆盖LLM生成的dialogue格式**
   - LLM可能使用不同的dialogue结构（如字符串数组而非对象数组）
   - 覆盖逻辑只处理了 `scene.dialogue.lines`，但LLM可能输出 `scene.lines` 或其他格式

#### 修复状态

- 预生产脚本已添加 `characters` 数组 (v1.2.6)
- 但 `_parseLLMResponse` 的覆盖逻辑是否完全有效，**待验证**

---

### 3.4 问题4: LLM temperature参数非法 (P2)

#### 背景

Stage 5B（visualPrompt优化）调用LLM时传入了非1的temperature值，导致API拒绝。

#### 错误日志

```json
{"error":{"message":"Invalid request Error","type":"invalid_request_error"}}
```

#### 根因

某些LLM模型（如部分版本的kimi-k2p6）要求temperature必须为1，但系统配置了其他值。

#### 代码位置

```javascript
// script-generator.js:338-339
max_tokens: this.config.maxTokens,
temperature: this.config.temperature  // 可能不是1
```

---

### 3.5 问题5: 空内容/推理内容分离 (P2)

#### 背景

LLM引擎返回的内容中，`content` 为空，但 `reasoning_content` 有值。系统需要正确处理这种情况。

#### 处理代码

```javascript
// script-generator.js:305-316
if (result.content && result.content.trim()) {
  return result.content.trim();
}
if (result.reasoning_content && result.reasoning_content.trim()) {
  console.warn('[ScriptGenerator] 从reasoning_content提取内容');
  return result.reasoning_content.trim();
}
throw new Error('LLM返回空内容（success=true但content为空）');
```

#### 问题

`reasoning_content` 通常包含思考过程而非正式输出，直接提取可能导致格式错误。

---

### 3.6 问题6: JSON截断解析脆弱性 (P3)

#### 背景

LLM可能输出截断的JSON（特别是当内容很长时），系统需要能够从中提取有效JSON。

#### 当前处理

```javascript
// script-generator.js:374-388
_tryParseJson(jsonStr) {
  try {
    return JSON.parse(jsonStr);
  } catch (e) {
    return null;
  }
}

_extractValidJson(jsonStr) {
  // 尝试提取最长有效JSON
  // ... 实现细节 ...
}
```

#### 问题

`_extractValidJson` 的实现可能不够健壮，对于复杂嵌套JSON截断情况处理不佳。

---

### 3.7 问题7: 山海经IP硬编码残留 (P3)

#### 背景

系统最初为"山海经"系列开发，后要求通用化。但代码中仍有大量山海经相关硬编码。

#### 已清理 (v1.2.5-alpha)

- 193处山海经硬编码文本已清理
- `nirath` 相关模板和扩展已隔离

#### 仍存在的残留

- `field-guard.js` 中 `beastVoice`、`openingHook` 等字段名
- `field-standardizer.js` 中 `beast-archive` 相关引用
- 部分模板文件仍包含山海经特定术语

---

## 4. 关键代码片段

### 4.1 标准输出字段定义 (v6.37)

```javascript
// engines/production-engine/production-engine.js:860-980
_engineerPrompts(shots, adapted) {
  const standardOutput = {
    // 标准字段（v6.37-production+）
    shotId: shot.shotId,
    duration: shot.timing?.duration || 20,
    scene: shot.scene,
    mood: shot.mood,
    camera: shot.camera,
    cameraString: shot.camera?.string || '',
    lighting: shot.lighting,
    lightingString: shot.lighting?.string || '',
    characterRef: shot.characterRef || 'NONE',
    character: shot.character || 'NONE',
    action: shot.action || 'NONE',
    dialogue: shot.dialogue || 'NONE',
    timeline: timelineObj,
    timelineString: timelineObj?.string || '',
    backgroundSound: backgroundSoundObj,
    backgroundSoundString: backgroundSoundObj?.string || '',
    prompt: finalPrompt,
    promptCharCount: finalPrompt.length
  };
}
```

### 4.2 质量门检查 (修复后 v1.2.6)

```javascript
// engines/production-engine/production-engine.js:1496-1535
_runQualityGate(prompts) {
  for (const p of prompts) {
    const check = {
      hasScene: !!p.scene && p.scene.length > 10,
      hasMood: !!p.mood && p.mood.split(',').length >= 3,
      hasCamera: !!(p.cameraString || p.camera) && (p.cameraString || p.camera).toString().length > 10,
      hasLighting: !!(p.lightingString) && p.lightingString.includes('K'),
      hasTimeline: !!(p.timelineString) && p.timelineString.includes('T00:'),
      hasBackgroundSound: !!(p.backgroundSoundString) && p.backgroundSoundString.includes('AMBIENT:'),
      // ...
    };
  }
}
```

### 4.3 角色覆盖逻辑

```javascript
// engines/script-engine/core/script-generator.js:398-424
const metadataChars = userIntent.metadata?.characters || [];
if (metadataChars.length > 0) {
  const overrideCharacters = metadataChars.map(c => ({
    character_id: c.id || c.name,
    name: c.name,
    role: c.role || 'protagonist',
    // ...
  }));
  
  parsed.character_system = { characters: overrideCharacters };
  
  if (parsed.structure?.scenes) {
    for (const scene of parsed.structure.scenes) {
      if (scene.characters) {
        scene.characters = overrideCharacters.map(c => c.character_id);
      }
      if (scene.dialogue?.lines) {
        for (const line of scene.dialogue.lines) {
          line.speaker = overrideCharacters[0]?.name || line.speaker;
        }
      }
    }
  }
}
```

---

## 5. 错误日志与报错

### 5.1 第二集预生产输出 (v1.2.5)

```
🔥 [HyperrealitySystem v1.2.5] 开始创作
   意图: 穿警服的陈卓女士...
   项目: 第二集：为什么会发生横纹肌溶解

📋 [Layer 0] 需求清单生成 - 解析用户意图...
   ✅ IntentParser 分类: educational (置信度: 1)
   ✅ 需求清单生成完成 (3ms)

📖 [Layer 1] 剧本引擎 - 生成结构化剧本...
[ScriptEngine] 使用LLMEngine (kimi-k2p6)
[ScriptGenerator] 模板加载失败: educational-template.json, 使用默认模板
   ✅ 剧本生成完成 (195s)
      场景: 6 | 角色: 2 | 台词: 16
      校验: 通过 (100分)

🎬 [Layer 2] 制作引擎 - 生成镜头...
   ✅ 制作完成 (7ms)
      镜头: 6 | Prompts: 6

🛡️ [FieldGuard] Layer2-Production warnings:
- Empty critical array: portraits
- Empty critical array: characterCards
(共12次)

[Hyperreality] Layer2-Production shot summary:
{
  "shotId": "SC00",
  "sceneType": "establishing",
  "dialogueCount": 0,
  "portraitCount": 0,
  "timelineCount": 0,
  "cardCount": 0
}
...
```

### 5.2 质量门失败详情

```json
{
  "passed": false,
  "passedCount": 0,
  "totalPrompts": 6,
  "failedFields": [
    {
      "shotId": "SC00",
      "failed": ["hasLighting", "hasTimeline", "hasBackgroundSound"]
    },
    // ... 所有6个镜头同样错误
  ]
}
```

### 5.3 角色错误详情

生成的角色：`小G`(医生) + `小R`(患者)  
期望角色：`陈卓`(穿警服的科普主讲人)

```json
{
  "characterRef": "小G: image://characters/xiaoG-front.png... | 小R: image://characters/xiaoR-front.png...",
  "character": "小G: human, 白大褂, 短发利落, 佩戴听诊器 | 小R: human, 运动服, 背着健身包, 年轻有活力",
  "dialogue": "xiaoG|独白|serious|过度运动、挤压或缺血，肌细胞会撑不住。..."
}
```

---

## 6. 期望结果

### 6.1 短期目标

1. **质量门100%通过**: 所有镜头检查项通过
2. **角色一致性**: LLM严格使用metadata指定的角色
3. **字段标准化**: 所有输出严格符合v6.37标准
4. **报告完整性**: Markdown报告正常生成，非"TODO"占位符

### 6.2 中期目标

1. **系统稳定性**: LLM调用失败率 < 5%
2. **Prompt质量**: PromptForge质量分 > 90
3. **通用性**: 彻底清理所有IP硬编码
4. **可维护性**: 字段变更只需修改一处

### 6.3 长期目标

1. **端到端自动化**: 从意图输入到成片交付全程无人干预
2. **多模态支持**: 支持图片/视频/音频多种输入
3. **A/B测试**: 支持多版本Prompt自动对比
4. **监控告警**: 实时质量监控与自动降级

---

## 7. 附录：版本历史

| 版本 | 日期 | 主要修复 |
|------|------|----------|
| v1.2.6 | 2026-06-19 | 质量门字段检查修复、时长显示修复、FieldGuard清理 |
| v1.2.5 | 2026-06-19 | 字段标准化、删除剧本确认环节、角色覆盖 |
| v1.2.5-fix5 | 2026-06-19 | 删除剧本确认环节 |
| v1.2.5-fix4 | 2026-06-19 | maxTokens扩至32000、超时扩至300s |
| v1.2.5-fix3 | 2026-06-19 | 统一metadata读取路径 |
| v1.2.5-fix2 | 2026-06-19 | 确认等待时间扩至2小时 |
| v1.2.5-fix | 2026-06-19 | 外部确认机制+默认横屏16:9 |
| v1.2.4 | 2026-06-18 | FieldGuard片头验证、剧本引擎超时、角色覆盖 |

---

> 本报告由系统自动生成，包含截至 v1.2.6 的所有已知问题。  
> 专家分析后请将结论和修复建议同步，我们将评估后纳入下一版本修复计划。
